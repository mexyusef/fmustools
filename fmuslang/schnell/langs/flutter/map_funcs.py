# kumpulan function calls
# map_funcs = {
# void main() { runApp(MyApp()); }
# void main() => runApp(MyApp())
# Navigator.of(context).pop()
# Navigator.of(context).push(MaterialPageRoute(builder: (_)=>OtherScreen())
# Navigator.pop(context)
# Navigator.push(context,MaterialPageRoute(builder: (_)=>OtherScreen())
# SystemChrome.setPreferredOrientations([DeviceOrientation.portraitUp])
# ListView.builder()
# PageView.builder()
# Rect.fromLTWH(gameController.screenSize.width / 2 - barWidth / 2,gameController.screenSize.height * 0.8,barWidth,gameController.tileSize * 0.5,);
# GridView.count(crossAxisCount: responsiveNumGridTiles(mediaQuery),mainAxisSpacing: 30.0,crossAxisSpacing: 30.0,shrinkWrap: true,physics: NeverScrollableScrollPhysics(),children: tiles,)


# _buildWidget(int index) {
#   return Text('$index');
# }

# "carousel" : ""
# ListView.builder(
#   padding: EdgeInsets.symmetric(horizontal: 10.0,),
#   scrollDirection: Axis.horizontal,
#   itemCount: products.length,
#   itemBuilder: (BuildContext context, int index) {
#     return _buildWidget(index);
#   }
# )

